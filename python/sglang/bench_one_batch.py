"""CLI entry point for ``python -m sglang.bench_one_batch``.

The implementation lives in ``sglang.benchmark.one_batch``; import from there.
"""

from sglang.benchmark.one_batch import cli_main

if __name__ == "__main__":
    cli_main()
